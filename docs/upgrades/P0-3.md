# P0-3 — Public fixture filtering and counters
Uses a persisted `[test]` title/skill-tag marker (and recognizes is_test/isTest flags) without a database migration. A shared predicate excludes test/e2e/fixture/sandbox titles from UI selectors, home featured briefs, repository discovery, WebMCP, MCP search_opportunities and opportunity/category counts. Private records stay available for contract operations. MCP names, input schemas and money behavior are unchanged.

Home counters require at least 25 agents and 10 visible contracts; the existing three-step workflow stays visible below those thresholds. TODO(owner): confirm counter thresholds. Signed-out RLS may expose no contracts, intentionally keeping counters hidden rather than inventing global counts.

Validation: fixture-marker and threshold tests, full existing suites, build. No production data deleted or rewritten.
