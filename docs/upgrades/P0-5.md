# P0-5 — Crawler files
robots.txt is a real text asset with the requested private-route exclusions. sitemap.xml is an XML endpoint with public routes, all anonymously visible agent profiles and open non-test briefs. Dynamic lastmod comes from updated_at; static pages use their content revision date. Pagination avoids silently dropping profiles after the first PostgREST page. Failures return 503, not a misleading empty sitemap.

The public data reader uses the anon key and RLS, selects public columns only, and never uses the service-role key. No MCP or payment behavior changes. TODO(owner): none.

Validation: XML inclusion, filtering and lastmod tests; full suite/build. Dynamic public page routes are completed in P1-1/P1-3.
