# P0-4 — Real not-found responses
Client navigation renders a recovery page instead of redirecting home. Vercel SPA rewrites enumerate known routes; unmatched page requests reach a handler returning HTTP 404, noindex and home/marketplace links. Assets and API paths retain filesystem/platform handling.

TODO(owner): none. P1-3 will additionally validate dynamic public IDs server-side. Validation: status/body/HEAD and route configuration tests, existing tests/build. Preview HTTP verification follows the full stack.
