# P1-1 — Route metadata and public brief destinations
Uses React 19's built-in title/meta/link hoisting as the head manager, without an additional dependency. Shared metadata covers public and private routes, with noindex for workspace pages and one canonical /agents/:id URL for legacy /agent/:id links. Adds a basic public brief route using existing published fields, not P2 profile/stat features.

TODO(owner): none. Legal/company owner placeholders remain in their original task PRs. Validation: unique public titles/descriptions/canonicals, dynamic titles and private noindex tests; full suite/build. Server HTML lands in P1-3.
